"""
Domain modules for Lead Intelligence (scraper, scoring, AI, exports).

These packages wrap or extend `services/` and top-level `modules/` so the FastAPI
layer stays thin and future subscription / worker processes can import one place.
"""
