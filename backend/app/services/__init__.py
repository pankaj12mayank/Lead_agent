"""
FastAPI-side service package (composition only).

Domain logic remains in the top-level `services/` package (lead_service,
scoring_service, message_service, etc.) — import that package from routes.
"""
