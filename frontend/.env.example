# Leave unset or empty for local dev: Vite proxies /api → http://127.0.0.1:8000
# (see vite.config.ts). For production builds, set the real browser-facing API URL.
# VITE_API_URL=
# VITE_API_URL=http://127.0.0.1:8000
